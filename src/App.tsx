const App = () => {
  const schedule = {
    title: "CS Course Schedule for 2025-26"
  };

  return (
    <div className="flex flex-col min-h-[80vh] items-center justify-center">
      <h1>{schedule.title}</h1>
    </div>
  )
}

export default App
